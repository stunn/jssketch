#!/usr/bin/env node

var program = require('commander');
var path = require('path');

var app = require('../lib/app');

program.option('-c, --config <file>', 'Path to the config file', path.join('..', 'config', 'config.json'))
       .parse(process.argv);

app(require(program.config)).start();